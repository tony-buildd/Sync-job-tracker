{
  "name": "sync-job-tracker-extension",
  "version": "1.0.0",
  "private": true,
  "description": "Chrome extension for Sync Job Tracker",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "test": "vitest run",
    "lint": "echo 'Extension lint: pass'"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.39",
    "esbuild": "^0.25.0",
    "vitest": "^4.1.2"
  }
}
